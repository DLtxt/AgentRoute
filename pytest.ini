[pytest]
# Make the repo root importable so `import app` works however pytest is
# invoked. `python -m pytest` adds the cwd to sys.path implicitly; the bare
# `pytest` console script does not, so without this the suite only passes
# under one of the two spellings.
pythonpath = .
asyncio_mode = auto
testpaths = tests
